/**
* @namespace Arith
*/
/**
* 可以在 `JavaScript` 中被表示的最小安全整数。
* @var {number} Arith.MIN_SAFE_INTEGER
*/

/**
* 可以在 `JavaScript` 中被表示的最大安全整数。
* @var {number} Arith.MAX_SAFE_INTEGER
*/

/**
* 可以在 `JavaScript` 中两个数字间可能被识别出的最小差值。
* @var {number} Arith.EPSILON
*/

/**
* 黄金分割比。约等于 `1.618` 。
*  @var {number} Arith.GOLDEN_RATIO
*/