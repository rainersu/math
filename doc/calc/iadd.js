
/**
* 以类 `C` 语言语法计算两个 `32` 位整数的加和。使用 `16` 位内操作以解决某些 `Javascript` 解译环境的 `Bug` 。
* @see {@link Arith.imul}
* @see {@link https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/imul|Math.imul}
* @access public
* @func Arith.iadd
* @param {number} x - 加数。
* @param {number} y - 被加数。
* @returns {number}
* @example
* console.log(Arith.iadd(20, 30));    // 50
*/