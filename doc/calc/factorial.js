/**
* 计算 `int` 的 {@link http://en.wikipedia.org/wiki/Factorial|阶乘} 。
* @see {@link Arith.fibonacci}
* @access public
* @func Arith.factorial
* @param {number} int - 要计算阶乘的自然数。
* @returns {number}
* @example
* console.log(Arith.factorial(0));    // 1
* console.log(Arith.factorial(1));    // 1
* console.log(Arith.factorial(3));    // 6
*/